/**
 * Pools feature — create a pool and join by share link.
 *
 * TODO(docs/DECISIONS.md#pool-join-flow): how the share link encodes the pool
 * (join code vs deep link) and what joining does on-chain vs off-chain.
 */

export function CreatePoolPage() {
  return null; // TODO: create-pool form -> create_pool instruction
}

export function JoinPoolPage() {
  return null; // TODO: join-by-link flow
}
