/**
 * Picks feature — pick UI plus kickoff lock.
 *
 * TODO(docs/DECISIONS.md#pick-data-model): what a pick is (1X2, exact score, both).
 * TODO(docs/DECISIONS.md#commit-strategy): plaintext vs commit-reveal.
 * TODO(docs/DECISIONS.md#kickoff-lock-source): where the UI lock time comes from
 *   (TxLINE fixtures) — the authoritative lock is on-chain.
 */

export function PicksPage() {
  return null; // TODO: pick form -> commit_picks instruction, disabled at kickoff
}
